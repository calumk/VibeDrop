# ============================================
# VibeDrop - Environment Variables
# ============================================

# App Configuration
VITE_APP_NAME=VibeDrop
VITE_APP_TAGLINE=Share files securely and magically
VITE_FAVICON_URL=https://your-domain.com/favicon.ico

# Simple Login Mode (set to 'true' to use simple login instead of splash page)
VITE_USE_SIMPLE_LOGIN=false

# Authentication
VITE_ADMIN_PASSWORD=your-secure-password
VITE_SIMPLE_AUTH_STRING=your-secret-auth-string

# Support the Developer (set to 'true' to hide the footer attribution)
VITE_I_HAVE_DONATED_TO_CALUMK=false

# ============================================
# Setup Instructions:
# ============================================
#
# For Local Development:
# 1. Copy this file to .env.local
# 2. Update the values with your actual credentials
# 3. R2 credentials are NOT needed in the frontend env file.
#
# For Cloudflare Workers:
# 1. Set R2 credentials as secrets using Wrangler:
#    wrangler secret put R2_ACCESS_KEY_ID
#    wrangler secret put R2_SECRET_ACCESS_KEY
#    wrangler secret put R2_BUCKET_NAME
#    wrangler secret put R2_PUBLIC_DOMAIN
#    wrangler secret put ACCOUNT_ID
#
# Note: All variables prefixed with VITE_ are exposed to the client-side
# Do NOT put sensitive backend credentials in VITE_ variables
#
# ============================================ 